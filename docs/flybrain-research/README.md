# FlyBrain literature research (2026-09-24)

This is a copy of the parallel literature review of the FlyBrain real-model findings. The original run directory is `/mnt/f/.flybrain/logs/research-20260924T2245Z/`.

- `SYNTHESIS.md` compares each finding (F1–F10) with prior work. It also covers methods to adopt, parallel efforts, roadmap implications, open questions, errata (section 6b) and the reference list.
- `research.json` holds the structured data: every source with its key result, the findings it links to, and its verification status.
- The `*.md` files are per-topic notes from the eight research agents.

**Verification.** An independent checker re-fetched 150 citations: 137 verified, 4 misattributed (corrected in the synthesis), 9 unverifiable (paywalled), 0 fabricated. Cite from the reference list, not from memory. Claims marked `[unverifiable]` are context only.

This copy is a snapshot. If the review is re-run, add a new dated directory instead of editing these files in place.
